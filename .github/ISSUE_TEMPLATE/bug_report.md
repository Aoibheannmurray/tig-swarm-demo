---
name: Bug report
about: Something broke — the kind of issue we prioritize
labels: bug
---

<!--
Bugs are what we fix. The three fields below are what make a report
actionable — a report without them will likely sit until someone can
reproduce it from scratch, which may be never.
-->

**What did you run, and what happened?**

```
# the exact command, and the output / traceback (trim secrets: keys, passwords)
```

**What did you expect instead?**


**Environment**

- OS (and WSL yes/no on Windows):
- Python version (`python3 --version`):
- Role: host / contributor / both
- Compute: `c3` / `local` Docker
- Provider(s) in `fleet.config.json` (no keys!):
- Commit or branch (`git rev-parse --short HEAD`):
