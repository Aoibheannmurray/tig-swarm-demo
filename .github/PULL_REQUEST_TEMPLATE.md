<!--
Small, focused bug fixes with a test are what get merged here — large
features are better carried in a fork (see CONTRIBUTING.md, "What to
expect from maintenance"). Opening a big PR anyway is fine, but expect
a slow or negative answer.
-->

**What does this fix?**


**How was it verified?**

<!-- e.g. `python scripts/test_foo.py` output, or the manual flow you ran.
     New behavior needs a self-running test_*.py / vitest case beside it —
     CI runs every scripts/test_*.py and server/test_*.py automatically. -->
